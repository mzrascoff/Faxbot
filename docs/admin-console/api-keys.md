# API Keys

Create, rotate, revoke DB-backed API keys for REST access.

- Create key with scopes (`fax:send`, `fax:read`, `inbound:list`, `inbound:read`, `keys:manage`)
- Copy token once; rotate and revoke from the same screen
- Use Send Fax to smoke test with the new key
